server:
  port: 8084

spring:
  application:
    name: adminservice
  datasource:
    url: jdbc:mysql://localhost:3306/shopsphere_admin
    username: root
    password: Then@meisvir1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true